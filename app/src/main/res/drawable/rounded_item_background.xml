<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?android:attr/colorControlHighlight"> <!-- Ripple effect for clicks -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <solid android:color="@color/light_grey_subtle" /> <!-- NEW: Subtle grey background -->
            <stroke android:width="1dp" android:color="@color/light_grey" /> <!-- Existing light border -->
        </shape>
    </item>
</ripple>